
  # EHR System Design Overview

  This is a code bundle for EHR System Design Overview. The original project is available at https://www.figma.com/design/5LXIfyOUpJDWNe94skZ7Om/EHR-System-Design-Overview.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  